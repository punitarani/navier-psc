{
	"name": "server",
	"private": true,
	"scripts": {
		"dev": "poetry run python main.py",
		"build": "echo 'Can not build a Python package'",
		"lint": "poetry run ruff check --fix .",
		"format": "poetry run ruff format ."
	}
}
